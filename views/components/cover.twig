{{ lazy ? '<img loading="lazy"' : '<img' }} class="{{ class }}__image" width="{{ post.thumbnail.sizes.medium_large.width }}" height="{{ post.thumbnail.sizes.medium_large.height }}" src="{{ post.thumbnail.src('medium_large') }}" srcset="{{ post.thumbnail.srcset }}" sizes="(max-width: {{ post.thumbnail.sizes.medium_large.width ~ 'px' }}) 100vw, {{ post.thumbnail.sizes.medium_large.width ~ 'px' }}" alt="{{ post.title }}" />
