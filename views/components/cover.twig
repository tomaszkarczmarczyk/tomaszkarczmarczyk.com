{{ lazy ? '<img loading="lazy"' : '<img' }} class="{{ class }}__image" width="{{ post.thumbnail.sizes.large.width }}" height="{{ post.thumbnail.sizes.large.height }}" src="{{ post.thumbnail.src('large') }}" srcset="{{ post.thumbnail.srcset }}" sizes="(max-width: {{ post.thumbnail.sizes.large.width ~ 'px' }}) 100vw, {{ post.thumbnail.sizes.large.width ~ 'px' }}" alt="{{ post.title }}" />
